{
  "name": "@financial-slides/extraction-benchmark",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "node src/cli.mjs --deterministic",
    "benchmark:live": "node src/cli.mjs --live",
    "check": "node --check src/cli.mjs && node --check src/metrics.mjs && node --check src/providers.mjs && node --check src/runner.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@financial-slides/contracts": "workspace:*"
  }
}
